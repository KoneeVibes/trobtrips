export type HeaderProps = {
    bg: string,
    leadText: string,
    highlightText: string,
    subLeadText: string
};

export type HeroProps = {
    bgimage: string
};

export type BaseButtonProps = {
    location: string
}